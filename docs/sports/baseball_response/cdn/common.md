# CDN MLB Response Docs

CDN examples for MLB game packages and scoreboard views. `?xhr=1` is required for JSON-like game-package payloads on most routes.

Verified with live MLB requests on 2026-05-08.
